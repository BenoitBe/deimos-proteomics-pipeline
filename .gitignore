# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/

# Pipeline outputs (generated at runtime — never commit)
proteomics_output/
*.xlsx
*.tiff
*.tif
last_config.yaml
last_config.json

# Temporary figures generated by the pipeline
scatter_*.png
volc_*.png
wgcna_*.png
heatmap_*.png
plt_*.png
upset_*.png
all_volcano_*.png
LOL_*.png
CHORD_*.png
frequence.png
plot_pca*.png
plot_umap*.png

# Dashboard JS bundles (too large for the repo — see requirements.txt for URLs)
chart.umd.min.js
xlsx.full.min.js

# Real project data at root only (keep example_data/ subdirectory)
/report.pg_matrix.tsv
/report.pr_matrix.tsv
/ExperimentalDesign.csv

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
